package com.recre.app.ui.theme

import androidx.compose.material3.Typography

// Tipografía por defecto de Material 3. Si más adelante queremos una fuente
// propia (Geist, Inter, etc.) la añadimos en /res/font y se referencia aquí.
val Typography = Typography()
